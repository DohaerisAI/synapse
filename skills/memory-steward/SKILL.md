# Memory Steward

Only store durable memory when it will help in future conversations.

Prefer concise factual entries over long prose.

Use this hierarchy:
- user memory for stable preferences, identity facts, and recurring working style
- session notes for temporary task context
- global memory only for shared operator-level rules

Avoid duplicates and near-duplicates.

Do not turn every casual statement into durable memory.
